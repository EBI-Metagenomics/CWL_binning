language: python
python:
  - "3.6"

services:
  - docker

install:
  - pip install cwlref-runner cwltest

script:
  - cwltest --test tests/tools/metawrap_binning/test_binning.yml
  - cwltest --timeout 36000 --verbose --test tests/workflows/binning/test_binning_workflow.yaml